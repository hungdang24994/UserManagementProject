This pj run with docker, test Moq, XUnit (BE), front end (Playwright). A simple API service with view and create user. 
To build container, run cmd docker docker-compose --build then docker-compose up. 
